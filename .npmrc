# Force npm to include all optional dependencies for all platforms.
# Without this, Windows npm skips Linux-only optional packages (e.g. @emnapi/core,
# @emnapi/runtime used by react-native-worklets), causing `npm ci` to fail on EAS
# Linux build servers because those packages are missing from the lock file.
include=optional
